import { PageTemplate } from '@/components/PageTemplate';

export default function SeriesPage() {
  return <PageTemplate title="Series" subtitle="Manage your teaching series, seasons, and episodes." />;
}
