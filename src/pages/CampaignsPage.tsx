import { PageTemplate } from '@/components/PageTemplate';

export default function CampaignsPage() {
  return <PageTemplate title="Campaigns" subtitle="Track and optimize active ministry campaigns." />;
}
